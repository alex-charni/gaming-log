import { ViewportEnter } from './viewport-enter';

describe('ViewportEnter', () => {
  it('should create an instance', () => {
    const directive = new ViewportEnter();
    expect(directive).toBeTruthy();
  });
});
