import { PulseOnClickDirective } from './pulse-on-click';

describe('PulseOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PulseOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
